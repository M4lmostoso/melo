# CLA Signatures

This branch stores the Contributor License Agreement signatures collected by the
CLA Assistant workflow (signatures/version1/cla.json). It is kept separate from
`main` so the CLA bot can commit signatures without being blocked by main's
required status checks. Do NOT add branch protection to this branch.